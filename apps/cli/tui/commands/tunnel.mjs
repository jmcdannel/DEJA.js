/**
 * scripts/tui/commands/tunnel.mjs
 * Tunnel commands removed — tunnel is managed via Settings panel.
 */

export function registerTunnelCommands() {
  // No-op: tunnel toggle is now in the Settings panel
}
