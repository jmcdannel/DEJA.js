---
title: Installation
description: How to clone, install, and configure DEJA.js for local development or production deployment.
section: docs
order: 2
---

# Installation

This guide walks you through setting up DEJA.js on your machine. By the end, you will have the full monorepo installed, configured, and ready to run.

## Prerequisites

Make sure the following tools are installed before proceeding:

| Requirement | Minimum Version | How to Check |
|-------------|-----------------|--------------|
| **Node.js** | 20.0 or later | `node --version` |
| **pnpm** | 9.0 or later | `pnpm --version` |
| **Git** | Any recent version | `git --version` |

If you do not have pnpm installed, you can enable it through Node.js corepack:

```bash
corepack enable
corepack prepare pnpm@9.0.0 --activate
```

A **DCC-EX CommandStation** connected via USB is optional during development. The server and frontend apps will run without hardware attached -- you just will not be able to send commands to the track.

## Clone the Repository

```bash
git clone https://github.com/jmcdannel/DEJA.js.git
cd DEJA.js
```

## Install Dependencies

DEJA.js uses pnpm workspaces with Turborepo. A single install command fetches dependencies for every app and package in the monorepo:

```bash
pnpm install
```

This may take a minute on the first run. Do not use `npm install` or `yarn` -- the project is configured exclusively for pnpm.

## Configure Environment Variables

Copy the example environment file to create your local configuration:

```bash
cp .env.example .env
```

Open `.env` in your editor and fill in the values. The table below describes each variable:

### Required Variables

| Variable | Description | Example |
|----------|-------------|---------|
| `LAYOUT_ID` | Your layout's unique identifier in Firebase. This scopes all data to your layout. | `my-home-layout` |
| `VITE_FIREBASE_API_KEY` | Firebase Web API key | `AIzaSy...` |
| `VITE_FIREBASE_AUTH_DOMAIN` | Firebase auth domain | `my-project.firebaseapp.com` |
| `VITE_FIREBASE_PROJECT_ID` | Firebase project ID | `my-project` |
| `VITE_FIREBASE_STORAGE_BUCKET` | Firebase storage bucket | `my-project.appspot.com` |
| `VITE_FIREBASE_MESSAGING_SENDER_ID` | Firebase messaging sender ID | `123456789` |
| `VITE_FIREBASE_APP_ID` | Firebase app ID | `1:123456789:web:abc123` |

### Communication Settings

| Variable | Description | Default |
|----------|-------------|---------|
| `ENABLE_WS` | Enable the WebSocket server | `true` |
| `ENABLE_MQTT` | Enable MQTT broker communication | `true` |
| `ENABLE_DEJACLOUD` | Enable Firebase cloud integration on the server | `true` |
| `VITE_WS_PORT` | WebSocket server port | `8082` |
| `VITE_WS_ID` | Server identifier string | `DEJA.js` |
| `VITE_MQTT_BROKER` | MQTT broker URL | `mqtt://localhost` |
| `VITE_MQTT_PORT` | MQTT broker port | `1883` |

### Optional Variables

| Variable | Description |
|----------|-------------|
| `VITE_SENTRY_DSN` | Sentry DSN for frontend error tracking |
| `SENTRY_DSN` | Sentry DSN for server-side error tracking |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob token for sound file storage (Sound API only) |

Individual apps can also have their own `.env.local` files in `apps/<app-name>/` for app-specific overrides.

## Firebase Setup

DEJA.js uses Firebase for real-time data synchronization between the browser apps and the server. You need a Firebase project with Firestore and Realtime Database enabled.

### 1. Create a Firebase Project

Go to the [Firebase Console](https://console.firebase.google.com/) and create a new project (or use an existing one). The free Spark plan is sufficient for development and small layouts.

### 2. Enable Services

In your Firebase project, enable these services:

- **Authentication** -- Enable at least the Email/Password sign-in provider.
- **Cloud Firestore** -- Create a database. Start in test mode for development.
- **Realtime Database** -- Create a database instance. Start in test mode for development.

### 3. Get Your Web App Config

In the Firebase Console, go to Project Settings and add a Web app. Copy the configuration values into the `VITE_FIREBASE_*` variables in your `.env` file.

![Firebase config location](/images/docs/installation/firebase-config.png)

### 4. Server-Side Access (Optional)

If you are running the DEJA.js server with `ENABLE_DEJACLOUD=true`, the server uses the Firebase Admin SDK. It authenticates using a service account key file or Application Default Credentials. See the [Firebase Admin SDK setup guide](https://firebase.google.com/docs/admin/setup) for details.

## Verify the Installation

Build all packages to confirm everything is wired up correctly:

```bash
pnpm build
```

If the build succeeds with no errors, your installation is complete. You can also run the type checker and linter to catch any configuration issues:

```bash
pnpm check-types
pnpm lint
```

## Troubleshooting

### `pnpm: command not found`

Make sure corepack is enabled (`corepack enable`) or install pnpm globally (`npm install -g pnpm@9`).

### Firebase errors on build

Double-check that all `VITE_FIREBASE_*` variables are set in `.env`. Missing values will cause build-time or runtime errors in apps that depend on Firebase.

### Serial port access denied

On Linux, your user may need to be in the `dialout` group to access USB serial ports:

```bash
sudo usermod -a -G dialout $USER
```

Log out and back in for the change to take effect.

### Port conflicts

If a dev server fails to start because a port is already in use, you can change the port in the app's `vite.config.ts` or stop the conflicting process.

## Next Steps

Your environment is ready. Head to the [Quick Start](/docs/quick-start) guide to launch the development servers and start controlling trains.
