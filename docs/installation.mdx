---
title: Installation
description: How to install DEJA.js — a single command to get the server running on your machine.
section: docs
order: 2
---

# Installation

Install DEJA.js with a single command that downloads the server, installs dependencies, and configures your environment.

This guide assumes you have already [signed up for DEJA Cloud](https://cloud.dejajs.com) and completed the onboarding wizard (see [Quick Start](/docs/quick-start), Steps 1–3).

## Prerequisites

| Requirement | Minimum Version | How to Check |
|-------------|-----------------|--------------|
| **Node.js** | 20.0 or later | `node --version` |

That's it — the install script handles everything else.

## Quick Install

Open a terminal on the machine connected to your DCC-EX CommandStation and run one command:

**macOS / Linux / Raspberry Pi**

```bash
curl -fsSL https://install.dejajs.com | bash
```

**Windows**

Windows users should install via [WSL](https://learn.microsoft.com/en-us/windows/wsl/install), then run the curl command above from a WSL terminal.

The script will:

1. Check for Node.js 20+ (provides install instructions if missing)
2. Prompt for your account credentials (UID and Layout ID from the onboarding wizard)
3. Create your environment configuration at `~/.deja/.env`
4. Detect serial ports for your DCC-EX CommandStation
5. Download the DEJA Server tarball and install dependencies
6. Install the `deja` CLI
7. Start the server

### `deja` CLI Commands

After installation, manage the server with the `deja` CLI:

| Command | Description |
|---------|-------------|
| `deja status` | Check server status, subscription, and serial connection |
| `deja logs` | View server logs |
| `deja logs -f` | Follow server logs in real time |
| `deja start` | Start the server |
| `deja stop` | Stop the server |
| `deja restart` | Restart the server |
| `deja update` | Download and install the latest server version |

### `~/.deja/` Directory Structure

The Quick Install places all files under `~/.deja/`:

```
~/.deja/
├── bin/
│   └── deja          # CLI executable (added to PATH)
├── server/           # Server files (downloaded tarball)
├── config.json       # Account config (uid, layoutId)
└── .env              # Environment variables (Firebase, ports, etc.)
```

After the script finishes, you're ready to [register your CommandStation](/docs/quick-start#step-5--register-your-commandstation).

## Verify the Installation

```bash
deja status
```

If the server is running and connected, you're good to go.

## Troubleshooting

### `deja: command not found`

The install script adds `~/.deja/bin` to your PATH. If the command is not found after installation, either open a new terminal or run:

```bash
source ~/.zshrc   # or source ~/.bashrc
```

### Firebase errors on startup

Double-check that all Firebase variables are set in `~/.deja/.env`. You can re-run the install script or edit the file manually. Your credentials are also available in the [Cloud app](https://cloud.dejajs.com) under **Settings > View Local Environment Configuration**.

### Serial port access denied

On Linux, your user may need to be in the `dialout` group to access USB serial ports:

```bash
sudo usermod -a -G dialout $USER
```

Log out and back in for the change to take effect.

## Next Steps

Your environment is ready. Head to the [Quick Start](/docs/quick-start) guide to register your CommandStation and start controlling trains.

## Looking for the developer setup?

If you want to contribute to DEJA.js or run the full monorepo locally, see [CONTRIBUTING.md](https://github.com/jmcdannel/DEJA.js/blob/main/CONTRIBUTING.md).
