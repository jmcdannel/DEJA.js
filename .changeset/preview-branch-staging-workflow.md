---
"dejajs-www": patch
---

improved: **[ci]** Add preview branch staging workflow — feature PRs now target `preview`, staging domains auto-update on merge, and only `preview → main` PRs require changesets and docs review
